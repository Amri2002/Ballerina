# Database Module

This module provides functions for user progress tracking and database operations for the backend service.

- `user_progress_mark_completed`: Marks a module as completed for a user.
- `get_user_progress`: Retrieves progress for a user.

Ensure this module is imported in your main service file as `import backend.database as database;`.
